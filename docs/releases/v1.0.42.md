# NodeBeacon 1.0.42 production acceptance

## Release decision

- Release date: 2026-08-09 (Asia/Shanghai)
- Application image: `nodebeacon:git-73e3613e40eb`
- Application commit: `73e3613e40eb3202f1c66d0b19a97e4ca9bed973`
- NodeBeacon Deployment revision: `84`
- Scope: two-stage password/GitHub authentication with NodeBeacon Authenticator
  TOTP, one-time recovery codes, local QR binding, challenge replay protection,
  TOTP management hardening, and the stale-challenge invalidation fix.

## Validation completed

- Local gates passed: `pnpm lint`, `pnpm typecheck`, 97 API tests,
  `pnpm build`, `pnpm exec playwright test --project=chromium` (34/34), and
  `git diff --check`.
- The production deployment script completed the immutable image build, k3s
  import, rollout, readiness, health, status API, authentication config, and
  anonymous admin guard checks.
- `scripts/verify-production.sh` passed after deployment with `1/1` available,
  all five nodes online, current backup freshness, required Prometheus rules,
  and the expected Cloudflare cache policy.
- The owner completed real-browser enrollment from `/admin/account`, saved the
  generated recovery codes, and confirmed both password + TOTP and GitHub +
  TOTP login paths in a separate private window. No secret, QR payload, or
  verification code was shared with the assistant.
- A fresh SQLite and node-registry backup was copied off-site after acceptance:
  `/var/backups/nodebeacon/nodebeacon-20260808T165703Z.tar.gz`.

## Evidence and rollback

- Release acceptance:
  `/root/deploy/nodebeacon-v1.0.42-20260808T165251Z/artifacts/deployments/20260808T165315Z-1.0.42-73e3613e40eb.txt`
- Final production verification:
  `/root/deploy/nodebeacon-current/artifacts/production-verification/20260808T165801Z-1.0.42-73e3613e40eb.txt`
- Current release checkout:
  `/root/deploy/nodebeacon-v1.0.42-20260808T165251Z`
- Current release link:
  `/root/deploy/nodebeacon-current -> /root/deploy/nodebeacon-v1.0.42-20260808T165251Z`
- Rollback: keep the acceptance backup available, restore it first if reverting
  the SQLite schema is required, then run
  `kubectl -n nodebeacon rollout undo deploy/nodebeacon` and rerun
  `./scripts/verify-production.sh` from the selected release checkout.
