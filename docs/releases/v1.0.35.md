# NodeBeacon 1.0.35 production acceptance

## Release decision

- Release date: 2026-07-30 (Asia/Shanghai)
- Application image: `nodebeacon:git-457e064d28a6`
- Application commit: `457e064d28a6145853ff7a70a1aead3a4f509a20`
- NodeBeacon Deployment revision: `74`
- Scope: align the authenticated node-management list with the compact,
  low-glare Komari operator-console direction: dark-default workspace, black
  shell, restrained navy table header, compact search and primary action, and
  matching mobile spacing. An explicitly saved light-theme preference remains
  respected.
- No API, SQLite schema, Kubernetes resource, Secret, or monitoring behavior
  changed in this release.

## Validation completed

- Local gates passed: `pnpm lint`, `pnpm typecheck`, 95 API tests, `pnpm build`,
  Chromium Playwright suite (27/27), Kubernetes kustomize render, and
  `git diff --check`.
- Chromium coverage verifies that a fresh authenticated admin session uses the
  dark operator theme and retains the intended 30px page heading, 50px search
  control, and navy table-header color.
- `scripts/deploy.sh --plan` and `scripts/deploy.sh` completed the immutable
  image build, k3s import, rollout, readiness, and smoke checks.
- `scripts/verify-production.sh` passed. Production Deployment is `1/1` Ready
  and the deployed image annotation points to the exact application commit
  above; all five nodes are online and backup/cache/anonymous-admin checks pass.
- A real Chromium production request to `/admin` correctly redirects to the
  protected Login page without accessing credentials. The deployed immutable
  stylesheet returns `200` with public immutable caching and contains the
  compiled deep-operator selector plus the `#06080c` shell and `#0e1731` table
  header color values.

## Evidence and rollback

- Release acceptance:
  `/root/deploy/nodebeacon-v1.0.35-457e064/artifacts/deployments/20260730T122007Z-1.0.35-457e064d28a6.txt`
- Production verification:
  `/root/deploy/nodebeacon-v1.0.35-457e064/artifacts/production-verification/20260730T122156Z-1.0.35-457e064d28a6.txt`
- Current release checkout:
  `/root/deploy/nodebeacon-v1.0.35-457e064`
- `/root/deploy/nodebeacon-current` points to the current release checkout.
- Rollback: `kubectl -n nodebeacon rollout undo deploy/nodebeacon`, then rerun
  the production verification checklist from `infra/README.md`.
