# NodeBeacon 1.0.37 production acceptance

## Release decision

- Release date: 2026-07-30 (Asia/Shanghai)
- Application image: `nodebeacon:git-3deafc333f3b`
- Application commit: `3deafc333f3b3d415988f0441b1c12a0ee0d8830`
- NodeBeacon Deployment revision: `76`
- Scope: align the admin node-list geometry with Komari's current admin source.
  The page now uses the full content width with 16 px padding, a 22 px heading,
  36 px controls, a 40 px table header, and 48 px data rows. Overview moved
  from the node-list content tabs into the sidebar while the legacy query URL
  continues to redirect to the canonical route.
- No API, SQLite schema, Kubernetes resource, Secret, or monitoring behavior
  changed in this release.

## Validation completed

- Komari source revision `5cdc033c8cce12df88c7da2795a4f64bbfecc00b`
  was inspected and its live 2048 px desktop geometry was measured before the
  NodeBeacon implementation was changed.
- Local gates passed: `pnpm lint`, `pnpm typecheck`, 95 API tests, `pnpm build`,
  Chromium Playwright suite (28/28), and `git diff --check`.
- Chromium regression coverage verifies the 2048 px layout has a 1776 px
  content table beside the 240 px sidebar, plus the exact heading, search,
  table-header, row, sidebar-navigation, theme, and mobile-drawer behavior.
- `scripts/deploy.sh --plan` and `scripts/deploy.sh` completed the immutable
  image build, k3s import, rollout, readiness, and smoke checks.
- `scripts/verify-production.sh` passed. Production Deployment is `1/1` Ready,
  all five nodes are online, and backup/cache/anonymous-admin checks pass.
- A fresh real Chromium production session loaded the new hashed JavaScript and
  CSS assets without page errors and correctly redirected anonymous `/admin`
  access to `/login`. The live CSS asset contains the released 16/36/40/48 px
  geometry rules.

## Evidence and rollback

- Release acceptance:
  `/root/deploy/nodebeacon-v1.0.37-3deafc3/artifacts/deployments/20260730T130212Z-1.0.37-3deafc333f3b.txt`
- Production verification:
  `/root/deploy/nodebeacon-v1.0.37-3deafc3/artifacts/production-verification/20260730T130330Z-1.0.37-3deafc333f3b.txt`
- Current release checkout:
  `/root/deploy/nodebeacon-v1.0.37-3deafc3`
- `/root/deploy/nodebeacon-current` points to the current release checkout.
- Rollback: `kubectl -n nodebeacon rollout undo deploy/nodebeacon`, then rerun
  the production verification checklist from `infra/README.md`.
