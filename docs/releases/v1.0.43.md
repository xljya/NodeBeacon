# NodeBeacon 1.0.43 production acceptance

## Release decision

- Release date: 2026-08-09 (Asia/Shanghai)
- Application image: `nodebeacon:git-c563b715306d`
- Application commit: `c563b715306df648a05e8ad474b2011cac22c903`
- NodeBeacon Deployment revision: `85`
- Scope: independent settings encryption key migration, Cloudflare source
  allowlist and real-IP restoration, UDP 8472 blocking, and default-drop host
  firewall baseline.

## Validation completed

- Local gates passed: `pnpm lint`, `pnpm typecheck`, 99 API tests,
  `pnpm build`, `pnpm exec playwright test --project=chromium` (34/34),
  `scripts/test-deploy-plan.sh`, and `git diff --check`.
- A fresh backup was created before the key change, copied off-site, and
  verified with `integrity=ok`, schema 6:
  `/var/backups/nodebeacon/nodebeacon-20260809T050748Z.tar.gz`.
- `SETTINGS_ENCRYPTION_KEY` was added to the existing Kubernetes Secret without
  replacing other keys. Startup migrated one legacy encrypted TOTP row; the
  post-deploy check confirmed the factor remains enabled and decryptable.
- nginx syntax passed before reload and after reload. The live config SHA256 is
  `381d58158454a7365c9e56ab222234164e47c816b2b63381036eb2d941e5a3e9`.
- Direct origin requests from the off-site VPS without or with a forged
  `CF-Connecting-IP` header were blocked; the public hostname returned 200.
- Host firewall validation: `policy drop`, explicit UDP 8472 drop, NodePort
  readiness 200, all 16 Pods running, Prometheus `up == 0` empty, and public
  TCP access remained available only for 80/443/9867.
- `scripts/verify-production.sh` passed with all five nodes online, fresh
  backup timestamp, required Prometheus rules, and expected cache headers.

## Evidence and rollback

- Release checkout:
  `/root/deploy/nodebeacon-v1.0.43-20260809T071500Z`
- Deploy acceptance:
  `/root/deploy/nodebeacon-v1.0.43-20260809T071500Z/artifacts/deployments/20260809T051235Z-1.0.43-c563b715306d.txt`
- Production verification:
  `/root/deploy/nodebeacon-v1.0.43-20260809T071500Z/artifacts/production-verification/20260809T051424Z-1.0.43-c563b715306d.txt`
- Previous nginx config:
  `/root/nginx-config-archive/monitor.liucf.com.conf.pre-1.0.43-20260809`
- Previous firewall config:
  `/root/nftables-config-archive/rs1000-k3s-guard.nft.pre-1.0.43-20260809`
- Rollback application code with
  `kubectl -n nodebeacon rollout undo deploy/nodebeacon`, then rerun
  `./scripts/verify-production.sh`. The new settings key may remain in the
  Secret because both 1.0.42 and 1.0.43 read it; restore the nginx/firewall
  files above only if reverting the host boundary as well.

## Follow-up

- Keep the Cloudflare IPv4/IPv6 lists synchronized with the committed nginx
  and nft files when Cloudflare publishes a range change.
- Schedule the remaining package/kernel maintenance separately; it was not
  bundled into this release.
