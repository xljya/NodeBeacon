apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: nodebeacon
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: nodebeacon
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
